name: dcdp_user_inpainting_box

conditioning:
  method: ps
  params:
    scale: 1

measurement:
  operator:
    name: inpainting

  mask_opt:
    mask_type: box
    mask_len_range: [128, 129]
    image_size: 256
    margin: [32, 32]

  noise:
    name: gaussian
    sigma: 0.05
